import React from 'react';
import ListMedia from '../components/ListMedia';

export default function ImageScreen({navigation}) {
  return (
    <ListMedia navigation={navigation} type='imagen'/>
  );
}